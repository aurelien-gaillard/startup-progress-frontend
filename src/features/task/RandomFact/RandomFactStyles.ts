import styled from 'styled-components'
import { theme } from '../../../theme'

export const StyledRandomFact = styled.div`
  padding-top: ${theme.sizes.s4};
  h2 {
    padding-bottom: ${theme.sizes.s1};
  }
`
